# Промты к каждому пункту ТЗ (MVP Laravel)  
**Проект:** Учебная платформа с поэтапным открытием уроков  
**Выбранное имя БД:** `studydb`

> Как пользоваться: копируй нужный промт целиком и отправляй в ИИ (ChatGPT/Codex/другой).  
> В каждом промте добавлен контекст: Laravel + Blade + MySQL/PostgreSQL, БД `studydb`.

---

## 1) Цель и результат

**Промт:**

```text
Ты senior product + tech analyst. На основе идеи учебной платформы на Laravel сформулируй раздел "Цель и результат" в формате короткого, четкого ТЗ.

Контекст:
- Стек: Laravel + Blade + MySQL/PostgreSQL
- Название БД: studydb
- Главная механика: урок N открывается только после завершения урока N-1

Сделай:
1) 3-5 предложений с формулировкой цели,
2) измеримый ожидаемый результат MVP,
3) 3 KPI (например, completion rate, время до завершения курса).

Ответ в Markdown, без воды.
```

---

## 2) Что должно работать (MVP)

**Промт:**

```text
Составь раздел "Что должно работать в MVP" для Laravel-проекта образовательной платформы.

Контекст:
- БД: studydb
- Роли: Student, Admin (минимально)
- Последовательное открытие уроков

Нужно:
- Список функциональных пунктов MVP (минимум 10)
- Для каждого пункта: "Что делает пользователь" + "Что проверяет сервер"
- В конце: критерий "MVP готов"

Формат: Markdown, нумерованный список.
```

---

## 3) Что не входит в MVP

**Промт:**

```text
Сформируй раздел "Что не входит в MVP" для учебной платформы на Laravel.

Контекст:
- БД: studydb
- Сейчас нужно быстро запустить ядро с unlock логикой уроков

Дай:
1) список out-of-scope (10-15 пунктов),
2) почему каждый пункт вынесен за MVP,
3) к какому этапу (Phase 2/3) его лучше отнести.

Формат: таблица Markdown (Функция | Почему не в MVP | Фаза).
```

---

## 4) Пользовательские роли

**Промт:**

```text
Опиши роли и права доступа для MVP учебной платформы на Laravel.

Контекст:
- БД: studydb
- Базовые роли: Student и Admin

Сделай:
- Матрицу прав (RBAC): действие vs роль
- Минимальные middleware/policy правила
- Риски неправильной авторизации и как их закрыть

Формат: Markdown + таблица прав.
```

---

## 5) Бизнес-правила доступа

**Промт:**

```text
Сформулируй строгие бизнес-правила доступа к урокам (sequential unlock) для Laravel-проекта.

Контекст:
- БД: studydb
- Уроки имеют position внутри курса
- Урок N доступен только при completion урока N-1

Нужно:
1) Формальные правила в формате "Если/То",
2) edge-cases (пропуски position, скрытый урок, удаленный урок),
3) fallback-логика для edge-cases,
4) что именно считать "урок завершен".

Формат: Markdown, разделы + bullet points.
```

---

## 6) Маршруты (пример)

**Промт:**

```text
Сгенерируй оптимальный набор web.php маршрутов для MVP-платформы на Laravel.

Контекст:
- БД: studydb
- Нужны страницы: список курсов, страница курса, урок, завершение урока

Требования:
- Показать полный блок Route::middleware(['auth'])
- Использовать route model binding
- Добавить названия маршрутов (name)
- Кратко пояснить назначение каждого маршрута

Формат: сначала код, потом краткое пояснение.
```

---

## 7) Структура БД

**Промт:**

```text
Ты Laravel architect. Спроектируй БД для MVP учебной платформы с unlock-логикой.

Контекст:
- Название БД: studydb
- Таблицы: courses, modules, lessons, user_lesson_progress

Нужно:
1) DDL-структура (поля, типы, nullable, default),
2) PK/FK/индексы/unique,
3) рекомендации по каскадам,
4) 3 примера SQL-запросов для проверки целостности,
5) замечания для MySQL и PostgreSQL (если есть различия).

Формат: Markdown + SQL блоки.
```

---

## 8) Eloquent-связи

**Промт:**

```text
Сгенерируй Eloquent-модели и связи для таблиц courses, modules, lessons, user_lesson_progress.

Контекст:
- БД: studydb
- Laravel 10/11

Нужно:
- Готовые фрагменты кода моделей
- Все связи (hasMany/belongsTo)
- scopes для published и ordered
- 2-3 полезных helper-метода (например isCompletedBy)

Формат: по модели отдельный код-блок + краткое пояснение.
```

---

## 9) Backend-логика

**Промт:**

```text
Распиши backend-логику Laravel для sequential unlock уроков.

Контекст:
- БД: studydb
- Completion хранится в user_lesson_progress

Сделай:
1) LessonAccessService (методы + ответственность),
2) CourseProgressService (расчет процента),
3) ProgressController@complete (идемпотентно через updateOrCreate),
4) валидации и проверки доступа,
5) обработку ошибок и тексты сообщений.

Формат: архитектурная схема + пример кода ключевых методов.
```

---

## 10) UI/UX требования

**Промт:**

```text
Сформируй практичные UI/UX требования для страницы курса и страницы урока в Blade.

Контекст:
- БД: studydb
- Статусы урока: completed/available/locked

Нужно:
- Четкие состояния интерфейса
- Тексты кнопок и сообщений
- Порядок элементов на странице
- Минимальные требования по доступности (a11y)

Формат: Markdown чек-лист + примеры микрокопирайта.
```

---

## 11) Безопасность и консистентность

**Промт:**

```text
Подготовь раздел "Безопасность и консистентность" для Laravel MVP платформы.

Контекст:
- БД: studydb
- Важны auth, доступ к урокам, целостность прогресса

Нужно:
1) список уязвимостей/рисков,
2) меры защиты (middleware, policy, validation, unique constraints),
3) защита от гонок при completion,
4) рекомендации по логированию событий.

Формат: таблица Риск | Последствие | Защита.
```

---

## 12) План работ (по шагам)

**Промт:**

```text
Составь детальный пошаговый план реализации MVP на Laravel (от пустого проекта до рабочего flow).

Контекст:
- БД: studydb
- Механика: sequential unlock уроков

Требования:
- 10-15 шагов
- Для каждого шага: цель, действия, результат, критерий завершения
- Отдельно указать зависимости между шагами

Формат: Markdown-таблица.
```

---

## 13) Acceptance Criteria

**Промт:**

```text
Сформируй строгие acceptance criteria для MVP учебной платформы.

Контекст:
- БД: studydb
- Ключевая фича: открытие уроков по порядку

Сделай:
- 12-20 критериев в формате Given/When/Then
- Покрыть auth, доступ к урокам, completion, прогресс, ошибки
- Добавить "Definition of Ready for release"

Формат: Markdown, нумерованный список.
```

---

## 14) Обязательные тест-кейсы

**Промт:**

```text
Сгенерируй набор обязательных тест-кейсов для Laravel Feature tests.

Контекст:
- БД: studydb
- Таблицы: courses/modules/lessons/user_lesson_progress

Нужно:
1) Не менее 20 тест-кейсов,
2) Позитивные/негативные/edge-case сценарии,
3) Для каждого: предусловия, шаги, ожидаемый результат,
4) Какие тесты автоматизировать в первую очередь.

Формат: Markdown-таблица + приоритизация P0/P1/P2.
```

---

## 15) Риски и меры

**Промт:**

```text
Составь риск-реестр для MVP образовательной платформы.

Контекст:
- БД: studydb
- Sequential unlock может ломаться из-за данных и логики

Нужно:
- Минимум 12 рисков (технические, продуктовые, UX)
- Для каждого: вероятность, влияние, mitigation, owner
- Топ-5 критичных рисков отдельно

Формат: Markdown-таблица + краткий вывод.
```

---

## 16) Оценка сроков MVP

**Промт:**

```text
Сделай реалистичную оценку сроков MVP для одного Laravel-разработчика middle уровня.

Контекст:
- БД: studydb
- Функциональность: sequential unlock + прогресс + базовый UI

Нужно:
1) оценка по этапам (часы/дни),
2) буфер рисков,
3) best case / likely case / worst case,
4) что можно упростить для ускорения релиза.

Формат: Markdown-таблица + итоговая рекомендация по сроку.
```

---

## 17) Definition of Done (DoD)

**Промт:**

```text
Сформируй практичный Definition of Done для релиза MVP.

Контекст:
- БД: studydb
- Проект: Laravel учебная платформа с unlock уроков

Нужно:
- Чек-лист DoD по категориям: код, БД, тесты, UX, безопасность, релиз
- Обязательные и желательные пункты
- "Стоп-факторы", при которых релиз запрещен

Формат: Markdown чек-лист с [ ] пунктами.
```

---

## 18) Универсальный мета-промт (если хочешь генерировать весь ТЗ по разделам автоматически)

**Промт:**

```text
Ты выступаешь как Lead Laravel Architect и Product Analyst.
Сгенерируй полный документ ТЗ для MVP учебной платформы с последовательным открытием уроков.

Контекст проекта:
- Стек: Laravel + Blade + MySQL/PostgreSQL
- Название БД: studydb
- Основная логика: урок N открывается только после завершения урока N-1
- Минимальные сущности: courses, modules, lessons, user_lesson_progress

Верни документ в разделах:
1) Цель и результат
2) Что должно работать (MVP)
3) Что не входит в MVP
4) Роли и права
5) Бизнес-правила доступа
6) Маршруты
7) Структура БД
8) Eloquent-связи
9) Backend-логика
10) UI/UX требования
11) Безопасность
12) План работ
13) Acceptance Criteria
14) Тест-кейсы
15) Риски
16) Сроки
17) Definition of Done

Требования к ответу:
- Только Markdown
- Конкретно, без воды
- С примерами кода там, где это полезно
- Учитывай production-практики Laravel
```
что использовать mysql или postgresql?